## Subir cambios a repositorio remoto

```
    git push
```
Sirve para pasar los archivos del repositorio local al remoto. Con "git push -u origin main" podremos mandar los archivos de la rama main al repositorio remoto.
